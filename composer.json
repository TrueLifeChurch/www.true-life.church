{
  "scripts": {
    "compile": [
      "composer create-project getgrav/grav ~/webroot/grav",
      "cd src; php bin/gpm install admin -y;",
      "cd src; php bin/gpm install devtools -y;",
      "cd src; php bin/gpm install zsimplex -y;"
    ]
  },
  "require": {
    "ext-mbstring": "*",
    "ext-gd": "*"
  }
}
