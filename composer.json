{
  "scripts": {
    "compile": [
      "composer create-project getgrav/grav ~/webroot/grav",
      "php -C -f src/bin/gpm install admin",
      "php -C -f src/bin/gpm install zsimplex"
    ]
  },
  "require": {
    "ext-mbstring": "*",
    "ext-gd": "*"
  }
}
