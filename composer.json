{
  "scripts": {
    "compile": [
      "composer create-project getgrav/grav ~/webroot/grav",
      "php -C -f src/bin/gpm install admin -y",
      "php -C -f src/bin/gpm install zsimplex -y"
    ]
  },
  "require": {
    "ext-mbstring": "*",
    "ext-gd": "*"
  }
}
