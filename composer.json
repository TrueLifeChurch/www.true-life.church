{
  "scripts": {
    "compile": [
      "composer create-project getgrav/grav ~/webroot/grav",
      "src/bin/gpm install admin -y",
      "src/bin/gpm install zsimplex -y"
    ]
  },
  "require": {
    "ext-mbstring": "*",
    "ext-gd": "*"
  }
}
